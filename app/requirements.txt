# Dependencies for the Hugging Face Space (free CPU tier).
#
# Kept separate from the training environment: the Space needs neither the CRF
# baseline nor the evaluation stack.
#
# The gradio upper bound is not cosmetic. gradio>=6 requires
# huggingface-hub>=1.16, while transformers<5 requires huggingface-hub<1.0.
# Installing them together silently upgrades the hub and breaks transformers at
# import time. Pin both or the Space fails to boot.
torch>=2.0
transformers>=4.44,<5
huggingface-hub>=0.34,<1.0
gradio>=5,<6
numpy>=1.24
